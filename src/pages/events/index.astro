---
import PageLayout from "@layouts/PageLayout.astro";
import { EventList, fetchEvents } from "@modules/events";
import { isFutureDate } from "@utils/formatDate";

const events = await fetchEvents();

// Separate upcoming and past events
const upcomingEvents = events.filter((e) => isFutureDate(e.date));
const pastEvents = events.filter((e) => !isFutureDate(e.date));
---

<PageLayout
  title="Formación Complementaria"
  description="Congresos, talleres, seminarios y capacitaciones que complementan la formación en Ingeniería Industrial de Jhoselin Quispe Luque."
>
  <!-- Hero -->
  <section class="container-custom section">
    <div class="mx-auto max-w-3xl text-center">
      <span
        class="mb-4 inline-block rounded-full bg-primary/10 px-3 py-1 text-sm font-medium text-primary"
      >
        Formación
      </span>
      <h1 class="mb-6 text-4xl font-bold text-foreground sm:text-5xl">Formación Complementaria</h1>
      <p class="text-xl text-muted-foreground">
        Congresos, talleres y seminarios que complementan mi formación académica en Ingeniería
        Industrial.
      </p>
    </div>
  </section>

  <!-- Stats -->
  <section class="container-custom pb-12">
    <div class="mx-auto grid max-w-2xl grid-cols-3 gap-4">
      <div class="rounded-2xl border border-border bg-card p-4 text-center sm:p-6">
        <p class="text-2xl font-bold text-primary sm:text-3xl">
          {events.length}
        </p>
        <p class="text-sm text-muted-foreground">Eventos</p>
      </div>
      <div class="rounded-2xl border border-border bg-card p-4 text-center sm:p-6">
        <p class="text-2xl font-bold text-accent sm:text-3xl">
          {upcomingEvents.length}
        </p>
        <p class="text-sm text-muted-foreground">Próximos</p>
      </div>
      <div class="rounded-2xl border border-border bg-card p-4 text-center sm:p-6">
        <p class="text-2xl font-bold text-success sm:text-3xl">
          {pastEvents.length}
        </p>
        <p class="text-sm text-muted-foreground">Completados</p>
      </div>
    </div>
  </section>

  <!-- Upcoming Events -->
  {
    upcomingEvents.length > 0 && (
      <section class="border-y border-border bg-gradient-to-br from-primary/5 via-accent/5 to-primary/5">
        <div class="container-custom section">
          <h2 class="mb-8 text-center text-2xl font-bold text-foreground">🎯 Próximos eventos</h2>
          <EventList events={upcomingEvents} showLink={false} />
        </div>
      </section>
    )
  }

  <!-- Past Events -->
  {
    pastEvents.length > 0 && (
      <section class="container-custom section">
        <h2 class="mb-8 text-center text-2xl font-bold text-foreground">📚 Eventos pasados</h2>
        <EventList events={pastEvents} showLink={false} />
      </section>
    )
  }

  <!-- Empty State -->
  {
    events.length === 0 && (
      <section class="container-custom py-20">
        <div class="text-center">
          <div class="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-primary/10">
            <svg
              class="h-10 w-10 text-primary"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="1.5"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5"
              />
            </svg>
          </div>
          <h3 class="mb-2 text-xl font-semibold text-foreground">Próximamente más eventos</h3>
          <p class="text-muted-foreground">Estoy preparando nuevos eventos. ¡Vuelve pronto!</p>
        </div>
      </section>
    )
  }

  <!-- CTA -->
  <section class="container-custom section">
    <div class="mx-auto max-w-3xl rounded-3xl border border-border bg-card p-8 text-center sm:p-12">
      <h2 class="mb-4 text-2xl font-bold text-foreground sm:text-3xl">
        ¿Te gustaría invitarme a dar una charla?
      </h2>
      <p class="mb-6 text-muted-foreground">
        Disfruto compartiendo conocimiento sobre mejora de procesos, gestión de calidad y análisis
        de datos.
      </p>
      <a
        href="/contact"
        class="inline-flex items-center gap-2 rounded-xl bg-primary px-6 py-3 font-medium text-primary-foreground transition-all duration-200 hover:bg-primary/90"
      >
        Contáctame
        <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path>
        </svg>
      </a>
    </div>
  </section>
</PageLayout>
